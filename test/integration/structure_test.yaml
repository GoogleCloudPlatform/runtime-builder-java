# Cloud Builder Pipeline that runs structure tests on a prebuilt image
# See https://github.com/GoogleCloudPlatform/runtimes-common/tree/master/structure_tests

steps:
- name: 'gcr.io/gcp-runtimes/structure_test'
  args: ['--image', '$_IMAGE_UNDER_TEST', '-v', '--config', '/workspace/$_CONFIG_PATH']
